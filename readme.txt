Test

cd test

git config --globa user.name "mariano"

git config --global user.email "email"

git init --> crea repo vacío

git status --> cambios

git add readme.txt

git commit -m "readme agregado al repo"


Tarea:

Crear una cuenta en github.